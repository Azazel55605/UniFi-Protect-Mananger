[package]
name = "protect-api-types"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared request/response types for protect-manager; the API contract"

[dependencies]
serde = { version = "1", features = ["derive"] }
ts-rs = "11"
